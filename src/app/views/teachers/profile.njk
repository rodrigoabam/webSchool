{% extends "layout.njk" %}

{% block content %}
<div class="card">
    <section class="foto"
    style="background: url({{ teacher.photo }}) no-repeat center center / cover"
    ></section>
    <section class="ficha">
        <h2>Professor</h2>
        <div class="item">
            <div>Nome</div>
            <div>{{ teacher.name }}</div>
        </div>
        <div class="item">
            <div>Idade</div>
            <div>{{ teacher.birth }}</div>
        </div>
        <div class="item">
            <div>Grau de escolaridade</div>
            <div>{{ teacher.scholarity }}</div>
        </div>
        <div class="item">
            <div>Modelo de aula</div>
            <div>{{ "Distância" if teacher.type_class == "D" else "Presencial" }}</div>
        </div>
        <div class="item">
            <div>Matérias ministradas</div>
            <div>
                {% for matter in teacher.matters %}
                <span>{{ matter }}</span>
                {% endfor %}
            </div>
        </div>

        <a href="/teachers/{{ teacher.id }}/edit" class="button">Editar</a>
    </section>
</div>
{% endblock %}